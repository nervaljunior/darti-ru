<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Recuperar Senha</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            background-color: #f4f4f4;
            background-image: linear-gradient(45deg, blue, cyan);
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .recover-password {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 300px; /* Largura máxima do contêiner */
            width: 100%;
            margin: auto; /* Centraliza o contêiner */
        }

        h1 {
            margin-bottom: 20px;
        }

        input {
            width: calc(100% - 20px); /* Adiciona um pouco de espaço para o campo de entrada */
            padding: 10px;
            margin-bottom: 15px;
            box-sizing: border-box;
        }

        button {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3; /* Tom mais escuro ao passar o mouse */
        }

        .login-link {
            margin-top: 10px;
            font-size: 14px;
            color: #333;
        }

        .login-link a {
            color: #007bff;
            text-decoration: none;
        }

        .login-link a:hover {
            text-decoration: underline;
        }

        .material-icons {
            vertical-align: middle;
        }
    </style>
</head>
<body>
{% block body %}


<div class="container">
    <h1>Hello {{ controller_name }}! ✅</h1>

    <div class="recover-password">
        <h1>Recuperar Senha</h1>
        <form action="#" method="post">
            <label for="email">E-mail:</label>
            <input type="email" id="email" name="email" required placeholder="E-mail">

            <button type="submit">Recuperar Senha</button>
        </form>
        <div class="login-link">
            <p>Lembrou a senha? <a href="{{path('app_logout')}}">Faça login <span class="material-icons">arrow_forward</span></a></p>
        </div>
    </div>
</div>
{% endblock %}
</body>

</html>