<!-- Adicione isto ao cabeçalho do seu HTML -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">

<button id="deleteButton" class="delete-btn" style="background-color: #dc3545; color: #fff; padding: 5px 12px; border: none; border-radius: 3px; cursor: pointer; margin-right: 5px;">
    <i class="fas fa-trash"></i>
</button>

<form id="deleteForm" method="post" action="{{ path('app_unidade_delete', {'id': unidade.id}) }}" style="display: none;" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unidade.id) }}">
</form>

<script>
    document.getElementById('deleteButton').addEventListener('click', function() {
        document.getElementById('deleteForm').submit();
    });
</script>
