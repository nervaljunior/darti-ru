{% extends 'base.html.twig' %}

{% block title %}Unidade{% endblock %}

{% block body %}
    <h1>Unidade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unidade.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ unidade.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_unidade_index') }}">back to list</a>

    <a href="{{ path('app_unidade_edit', {'id': unidade.id}) }}">edit</a>

    {{ include('unidade/_delete_form.html.twig') }}
{% endblock %}
