<form method="post" action="{{ path('app_cardapio_delete', {'id': cardapio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cardapio.id) }}">
    <button class="btn">Delete</button>
</form>
